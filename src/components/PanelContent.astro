---
interface Props {
  name?: string;
}

const { name } = Astro.props;
---

{
  name ? (
    <div class="panel-tab" id={`${name}`}>
      <slot />
    </div>
  ) : (
    <section class="panel-content">
      <slot />
    </section>
  )
}
